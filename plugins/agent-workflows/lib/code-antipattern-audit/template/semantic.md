# `code-antipattern-audit-semantic` Report

## Scope
- `scope`: `<fill repository-relative scope>`

## Report metadata
- `report_uuid`: `<fill uuidgen output>`
- `report_path`: `<fill repository-relative report path>`

## Reviewed anti-pattern cards
- `<fill each reviewed anti-pattern id or write None>`

## Collected signals
- `<fill one bullet per candidate signal with anti-pattern id, file path, line, observed signal, and scope expansion used, or write None>`

## Rejected signals
- `<fill one bullet per rejected signal with anti-pattern id, file path, line, competing cards rejected when relevant, exception status, and rejection reason, or write None>`

## Confirmed anti-pattern cases
- `<fill one bullet per confirmed case with anti-pattern ids, violated owner rule, file path, line, observed evidence, competing cards rejected, exception status, scope expansion used, and remediation direction, or write None>`

## Clean cards checked
- `<fill one bullet per reviewed anti-pattern id with inspected area and no confirmed case in the declared scope, or write None>`

## Verdict
- `overall_verdict`: `<CLEAN|FINDINGS|NO_AUDITABLE_SCOPE>`
